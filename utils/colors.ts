
export const colors = ['blue', 'red', 'yellow'];

export const calculateRandomColor = (index: number) => {
  // const random = Math.floor(Math.random() * Math.floor(this.data.length));
  // console.log('random number, ', random);
  return colors[index];
}